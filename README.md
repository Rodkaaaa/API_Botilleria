# API_Botilleria
como comenzar:
1. python -m venv venv #agregar venv
2. venv\Scripts\activate #activar venv
3. pip install -r requirements.txt #instalar requerimientos
4. uvicorn app.main:app --reload  #correr api